<h1 class="text-center" style="margin-bottom: 30px;">Editing - <%= @artist.name %></h1>

<div class="col-md-8 col-md-offset-2">
    <%= render 'form', artist: @artist %>
</div>

<div class="col-md-8 col-md-offset-2">
    <%= link_to 'Show', @artist, class: 'btn btn-info' %> |
    <%= link_to 'Back', artists_path, class: 'btn btn-default' %>
</div>