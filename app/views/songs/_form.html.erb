<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, id: :song_name, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :artist_id, class: 'form-label' %>
    <%# form.text_field :artist_id, id: :song_artist_id %>
    <%= form.collection_select :artist_id, Artist.all, :id, :name, {:prompt => 'Select Artist'}, {class: 'form-control'} %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-success' %>
  </div>
<% end %>
